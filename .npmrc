# madge (dev-only, used by `dependency:check`) declares an optional peer on
# typescript ^5.4.4, but the project runs typescript 6. The conflict is benign
# (madge only reads the tsconfig), so allow npm to install past it. This keeps
# `npm ci` working in CI and for anyone cloning the repo.
legacy-peer-deps=true
